<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_app2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_app2_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AsociaciónPedrajeraDePeñas"
        tools:targetApi="31">
        <activity
            android:name=".DetallePenaActivity"
            android:exported="false" />
        <activity
            android:name=".PantallaMapaActivity"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCjUvR8Fxu4mMQGOAHyIH5wFLJJnjd5vug" />

        <activity
            android:name=".PantallaAdministradorActivity"
            android:exported="false"
            android:label="@string/title_activity_pantalla_administrador"
            android:theme="@style/Theme.AsociaciónPedrajeraDePeñas.NoActionBar" />
        <activity
            android:name=".PantallaPrincipalActivity"
            android:exported="false"
            android:label="@string/title_activity_pantalla_principal" />
        <activity
            android:name=".RegistroActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>